-- V45__Delete_all_design_phase_data.sql
-- This migration was for cleaning up design phase data
-- Now handled as a no-op since data cleanup is done through the application

SELECT 'V45: Design phase data cleanup skipped - handled via application' AS migration_status;
