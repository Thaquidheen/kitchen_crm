/**
 * Product Components - Barrel Export
 * (The Sprint 6.3 experiments — ProductFilters, ProductCard, AdvancedProductForm,
 * CabinetManager, and the standalone accessory/cabinet/door API slices — were dead code
 * imported by nothing, deleted with the table redesign.)
 */

export { CategoryManager } from './CategoryManager';
export { BrandManager } from './BrandManager';
export { MaterialManager } from './MaterialManager';
